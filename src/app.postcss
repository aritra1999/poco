@tailwind base;
@tailwind components;
@tailwind utilities;


html {
    font-family: 'Rubik', sans-serif;
}

.input {
    @apply flex items-center justify-center py-3 px-5 bg-slate-100 dark:bg-slate-800 rounded-xl text-sm focus:ring-slate-900 focus:dark:ring-slate-100 hover:ring-slate-900 hover:dark:ring-slate-100;
}

input {
    @apply p-0 outline-0 ring-0 border-0 bg-transparent !important;
}
